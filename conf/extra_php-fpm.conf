pm = dynamic
pm.max_children = 50       ; Adjust higher if the server has more than 4GB RAM available
pm.start_servers = 10
pm.min_spare_servers = 5
pm.max_spare_servers = 15
pm.max_requests = 1000

memory_limit = 512M
